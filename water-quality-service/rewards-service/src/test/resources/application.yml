spring:
  application:
    name: rewards-service
server:
  port: 0
management:
  endpoints:
    web:
      exposure:
        include: health,info

rewards:
  data:
    base-url: http://localhost:8081
    batch-size: 50
  poll:
    ms: 5000
